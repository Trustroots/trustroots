# This will be loaded inside the `http { }` block

# Disable passenger versions
passenger_show_version_in_header off;

# Hide server identity headers
# https://stackoverflow.com/a/45028698/1984644
server_tokens off;
