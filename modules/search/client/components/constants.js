export const SOURCE_OFFERS = 'offers';
export const SOURCE_HEATMAP = 'offers-heatmap';

// Minimum and maximum zoom levels when offers are hidden and heatmap shown instead
export const HEATMAP_MIN_ZOOM = 2;
export const HEATMAP_MAX_ZOOM = 4;

// Zoom level buffer for opacity changes in layers relative to heatmap
export const HEATMAP_OPACITY_BUFFER = 0.3;
